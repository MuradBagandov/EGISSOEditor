<UserControl x:Class="EGISSOEditor.FolderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="UserControl_Loaded">
    <Grid Background="White">
        <ScrollViewer>
            <WrapPanel x:Name="WrapContent" Margin="3">
                <Button Width="90" Background="{x:Null}" BorderBrush="{x:Null}" VerticalAlignment="Top">
                    <StackPanel>
                        <Image Source="Resources\File_Excel.png" Width="50"></Image>
                        <TextBlock TextWrapping="Wrap" TextAlignment="Center" FontSize="10">1. ЗАЯВЛЕНИЕ УРМ АС Бюджет (Криста) + WEB-консолидация 2019.xlsx</TextBlock>
                    </StackPanel>
                </Button>
            </WrapPanel>
        </ScrollViewer>

    </Grid>
</UserControl>
